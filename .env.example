# Origen de datos de la aplicacion local (Vite).
#   excel      -> src/data/paneles.xlsx + localStorage (fase 1, sin red)
#   sharepoint -> solo tiene sentido dentro del web part de SPFx
VITE_DATA_SOURCE=excel
